# Auto-provision the Prometheus datasource so Grafana is usable on first boot
# with no manual clicking. Grafana loads this at startup.
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    uid: casq-prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
